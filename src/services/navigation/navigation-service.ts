import { Brand } from 'src/types/product/BrandTypes'

export function getLinkBrand(brand: Brand): string {
  return '/asics/b'
}

export function getLinkByType(item, type: string): string {
  return '/slug/b'
}
